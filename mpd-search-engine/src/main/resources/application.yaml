server:
  port: 9090

mpdback:
  defaultAddress: ws://localhost:8080/javaclient


management:
  endpoints:
    web:
      exposure:
        include: "*"

elasticsearch:
  defaultHost: localhost
  defaultPort: 9200
  defaultProtocol: http
  defaultIndex: mpd1

